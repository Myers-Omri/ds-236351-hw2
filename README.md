# ds-236351-hw2
## Running the project
To run the project, go to the project directory, run *mvn clean package*
Then run *java -jar target\BCS-1.0.jar*
A single server will start running the blockchain server and the HTTP server.
